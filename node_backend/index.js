// const express = require("express");
// const nodemon = require("nodemon");
// const cors = require("cors");
// const { mongoose } = require("./dbConnexion/cnnexion");

// const app = express();
// app.use(express.json());
// // express.json return les fichiers en format json


// app.use(cors())
// // cors verifie les requettes

// app.use(express.urlencoded({extended:true}));



// app.use((req,res, next)=>{
//     res.setHeader("Access-Control-Allow-Origin", "*");
//     res.setHeader("Access-Control-Allow-Headers", "Origin,X-Requested, Content, Accept, Content-Type, Authorization");
//     res.setHeader("Access-Control-Allow-Methods","GET, POST, DELETE, PATH, PUT, OPTIONS");
//     next();
// })


